  Game development

* <canvas></canvas> # render game inside
    <canvas id="myCanvas" width="480" height="320"></canvas>

* const canvas=document.getElementById("myCanvas");
  const ctx=canvas.getContext("2d");
  # to store the 2D rendering context

* ctx.beginPath();
  ctx.rect(20,40,50,50); # x,y,height,width of rectangle
  ctx.fillStyle = '#FF0000'; /"red" /'rgb(0 0 255 / 50%)'
  ctx.fill();
  ctx.closePath();

* ctx.arc(240,160,20,0,Math.PI*2,false); # x,y of arc center, arc radius, start and end angle in radians, direction of drawing(false for clockwise, default or anti-clockwise -true)

* ctx.stroke() # color only the outer stroke

* ctx.clearRect(x, y, a, b) # x and y coordinates of top left corner, and a and b coordinates of the bottom right corner of a rectangle, the whole area covered by this rectangle will be cleared of any content previously painted there

* document.location.reload() # reload page

* ctx.font = "16px Arial@"; 
  ctx.fillText(`Score: ${value}`, x, y); #draw text
* requestAnimationFrame(func) # Insert in func before last curle braces, func will be executed over and over, browser will sync the frame rate accordingly and render the shapes only when needed
